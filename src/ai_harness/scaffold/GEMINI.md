# Gemini / Antigravity

Follow root [`AGENTS.md`](AGENTS.md) for {{PROJECT_NAME}} harness (cold start, invariants, forbidden zones).

Hard deny hooks for Antigravity CLI: [`.agents/hooks.json`](.agents/hooks.json).
